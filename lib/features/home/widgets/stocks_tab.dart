import 'package:flutter/material.dart';

class StocksTab extends StatelessWidget {
  const StocksTab({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Stock Tab Content Here'),
    );
  }
}
